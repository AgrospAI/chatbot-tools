export const defaultLocale = "en"
export const locales = ["en", "es"] as const

export const localeLabels = {
  en: "English",
  es: "Español",
} as const

export const themeCookieName = "__theme__"
